zerops:
  - setup: backend
    build:
      base: nodejs@22
      buildCommands:
        - |
          cd backend
          npm install
      deployFiles:
        - ./backend

    run:
      base: nodejs@22
      ports:
        - port: 5000
          httpSupport: true
      start: cd backend && npm start