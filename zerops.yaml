zerops:
  - setup: backend

    build:
      base: nodejs@22
      buildCommands:
        - npm --prefix backend install
      deployFiles:
        - backend

    run:
      base: nodejs@22
      ports:
        - port: 5000
          httpSupport: true
      start: cd backend && npm start
