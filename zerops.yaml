zerops:
  - setup: backend

    build:
      base: nodejs@22
      buildCommands:
        - npm install
      deployFiles:
        - ./
      cache:
        - node_modules

    run:
      base: nodejs@22
      ports:
        - port: 5000
          httpSupport: true
      start: npm start